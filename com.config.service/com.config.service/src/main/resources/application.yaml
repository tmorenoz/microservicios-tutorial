server:
  port: 8081
Spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri:  https://github.com/tmorenoz/microservicios-tutorial
          search-paths: config-data
  application:
    name: config-service

